<% if @order.canceled? %>
  alert("非常抱歉，你没有选择技师\n本次服务取消\n点击确认再来一次");
  window.location.href = "/orders/new";
<% else %>
  index = "<%= j render "index" %>";
  if (content.innerHTML !== index) content.innerHTML = index;
<% end %>
