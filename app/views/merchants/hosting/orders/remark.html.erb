<h3 class="page-header">派单备注</h3>

<div class="row">
  <div class="col-md-7">
    <%= bootstrap_simple_form_for([ :merchants, @order ], url: remark_merchants_hosting_order_path(@order)) do |f| %>
      <%= f.error_messages %>

      <%= f.input :hosting_remark, input_html: { rows: 4 } %>

      <%= f.button :submit, "确定", class: "btn-primary pull-right" %>
    <% end %>
  </div>
</div>
